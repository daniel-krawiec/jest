const adding = (a, b) => a + b;
const multiplying = (a, b) => a * b;
const substracting = (a, b) => a - b;
const dividing = (a, b) => a / b;

module.exports = { adding, multiplying, substracting, dividing };
